# Shared-password gate (single user, no accounts)
# The password she will type to access the tool.
PREP_PASSWORD=change-me
# Random 32-byte hex string used to sign the auth cookie.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PREP_AUTH_SECRET=change-me
